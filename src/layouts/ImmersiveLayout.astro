---
import AsciiSiteTree from '@/components/core/AsciiSiteTree.astro';
import BaseHead from '@/components/core/BaseHead.astro';
import ThemeBootstrap from '@/components/core/ThemeBootstrap.astro';
import '@/styles/global.css';
import '@/styles/ascii-mode.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const { title, description, image } = Astro.props;
const currentPath = Astro.url.pathname;
const plainTitle = title?.split('|')[0].trim() || 'Immersive page';
---

<!doctype html>
<html
  lang="en"
  data-season="summer"
  data-season-preference="auto"
  data-theme="light"
  data-display-mode="default"
>
  <head>
    <ThemeBootstrap />
    <BaseHead {title} {description} {image} />
  </head>

  <body class="immersive-page" data-ascii-page="interior">
    <AsciiSiteTree {currentPath} />

    <main id="main-content">
      <div data-ascii-only>
        <h1>{plainTitle}</h1>
        {description && <p>{description}</p>}
      </div>
      <div data-default-only>
        <slot />
      </div>
    </main>

    <script>
      import { initializeDisplayMode } from '@/lib/display-mode';
      initializeDisplayMode();
    </script>
  </body>
</html>

<style is:global>
  html:not([data-display-mode='ascii']),
  html:not([data-display-mode='ascii']) body.immersive-page {
    width: 100%;
    height: 100%;
    margin: 0;
    overflow: hidden;
    background: #020202;
  }

  html:not([data-display-mode='ascii']) body.immersive-page #main-content,
  html:not([data-display-mode='ascii']) body.immersive-page [data-default-only] {
    width: 100%;
    height: 100%;
    min-height: 100%;
    margin: 0;
    padding: 0;
  }
</style>
